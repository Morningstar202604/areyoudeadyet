<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.silema.app.wear">

    <uses-feature android:name="android.hardware.type.watch" />
    
    <!-- BLE permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    
    <!-- Health Connect permissions -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_STEPS" />
    
    <!-- Wear OS Data Layer -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WearApplication"
        android:allowBackup="true"
        android:label="死了吗·手表端"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="死了吗·手表端"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Health Connect capability declaration -->
        <meta-data
            android:name="androidx.health.connect.client.capabilities"
            android:resource="@xml/health_connect_capabilities" />

        <!-- Wear OS Data Layer Listener Service -->
        <service
            android:name=".datalayer.WearDataLayerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/vital_records" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/heart_rate" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/sync_request" />
            </intent-filter>
        </service>

        <!-- Background Heart Rate Sync Service -->
        <service
            android:name=".service.HeartRateSyncService"
            android:exported="false" />
    </application>

</manifest>
