# 2026-08-27

## 「死了吗？」安卓 App — v0.5.0 视觉重设计闭环完成
- 设计系统 v2 落地：`Theme.kt`/`Widgets.kt` 令牌（AppShapes/AppSpacing/AppSize，深饱和达标对比度色板），保留旧导出名避免引用崩溃。
- 导航可达性修复：`AppNav` 底栏 5 项 + SOS 常驻红色 FAB（`FabPosition.End`）；新建 `MoreScreen` 功能中心收容其余入口。
- 全屏形状令牌化 + `items` key 补全 + 移除 `DashboardScreen` 问候语 emoji；新建 `Previews.kt`（10 屏 `@Preview`，`SilemaTheme` 下）。
- 编译验证：修复 4 个错误（AppNav 缺 `dp` 导入；`DashboardScreen`/`GuardianScreen` 的 `items` key lambda 引用内容 lambda 参数导致作用域错误）。
- `assembleRelease` 成功；`dist/silema-v0.5.0-release.apk` 已签名（证书 `CN=Silema AreYouDead`）并 apksigner 校验通过。
- 遗留：后端缺口——`AiAnalyzerProvider`/`RemoteSyncProvider` 仍永远返回 Mock（README 宣称的通义/智谱/文心真接入、远程同步未实现）；README 功能宣称与实现有偏差，建议后续单独对齐。

## 发布前收尾（同日晚间）
- README 对齐：远程监护/AI 分析标注「Mock 占位，未接真实后端」；屏幕数 12→11（加 MoreScreen 入结构列表）；英文 Features 同步。
- `dist/` 清理：删 v0.3.0/v0.4.0，仅留 v0.5.0。
- **算法测试门禁重大盲区修复**：根 `test/` 下的 3 个程序是带 `main` 的独立 Java 程序（非 JUnit），原不在 Android 单元测试源集，`testDebugUnitTest` 报 `NO-SOURCE`——之前「测试充分」是源码审阅结论，从未真正运行（假绿灯）。
  - `app/build.gradle.kts` 改造：`android.sourceSets["test"].java.srcDir(rootDir.resolve("test"))`（srcDir 相对 module 目录，必须用 rootDir 指回项目根）；`testImplementation("junit:junit:4.13.2")`；`afterEvaluate` 注册 `runAlgoTests`（3 个 `JavaExec`：TestEngine/TestStats/TestFeatures），classpath = test 编译产物 + `build/tmp/kotlin-classes/debug` + `build/intermediates/javac/debug/compileDebugJavaWithJavac/classes` + `debugRuntimeClasspath`。
  - 实测：`runAlgoTests` BUILD SUCCESSFUL，34 项全部 PASS（RiskEngine 四级/组合/连续升级、Stats MAP/SI/PPG、BleCodec SFLOAT、Features Haversine/周报等）。
  - 随后 `assembleRelease` 在当前配置下复验成功（2m14s），`dist/silema-v0.5.0-release.apk` 覆盖刷新、签名有效。
- **当前状态：发布前应处理事项已全部完成，待用户授权执行发布（推 GitCode + GitHub 双远程、附签名 APK）。**

## 继续推进：消除两个 Mock 占位（真实本地能力）
- 用户授权「先不用提交，先继续推进」，选方向＝补齐真实本地能力。
- `LocalAiAnalyzer.kt`（新建）：真实在端分析器，基于 `RiskEngine.evaluate()` 转译 `Assessment`→`HealthInsight`（风险评分/发现/建议），完全本地、无云端模型。删除 `MockAiAnalyzer.kt`，`AiAnalyzerProvider` 改返回 `LocalAiAnalyzer`。
- `LocalExportSync.kt`（新建）：真实本地同步，复用 `FhirExporter` 把体征落盘为 FHIR R4 Bundle 文件；`getFamilyMembers()` 返回空（诚实，不伪造）。删除 `MockRemoteSync.kt`，`RemoteSyncProvider` 改返回 `LocalExportSync`。
- `FamilyScreen` 空态文案改为本地模式诚实说明（导出分享可用，远程实时监护需后端）。README 能力表与英文 Features 同步改为真实本地。
- 持久化决策：用户确认**保留现有 JSON 离线持久化，不引入 Room**（构建环境脆弱 Kotlin 2.0.21 + Gradle 8.10，Room+KSP 有回归风险；JSON 已满足真实本地持久化目标）。
- 验证：`compileDebugKotlin` 通过；`runAlgoTests` 34/34 PASS；`assembleRelease` BUILD SUCCESSFUL；`dist/silema-v0.5.0-release.apk` 刷新且签名有效。
- 结论：v0.5.0 两个 Mock 占位已消除，功能真实可用。仍未 git 提交（用户要求先不提交）。

## 端架构重大更正 + :core 抽离 + 手表端从零建（同日晚间）
- 用户第 12 条纠正：手机端是「次端/守护端」（给监视老人家的人）；**主产品是手表端（Wear OS），给老人家佩戴**。此前误把两端都当手机，已绘正确拓扑给用户确认。
- 经核查：工程仅有 `:app` 手机模块，无任何 Wear OS 模块——手表端 0 代码（真实缺口，已如实告知用户）。
- 用户确认方向：① 从零建 `:wear` 模块（主产品，5 屏：健康首页/评估、SOS、运动、体征录入、AI 简报）；② 手机端改为纯守护端（去老人家自用屏）。
- `:core` 抽离（共享真实领域层）：把纯 Kotlin 文件移出 `:app` 进新建 `:core`——`data/Models.kt`、`data/RiskModels.kt`、`engine/RiskEngine.kt`、`engine/Stats.kt`、`engine/HealthReport.kt`、`medical/FhirExporter.kt`（均 0 个 android 依赖）。`:app`/`:wear` 均 `implementation(project(":core"))`。
  - **关键坑（已解决）**：`:core` 初版用 `com.android.library` 时，`debugRuntimeElements` 暴露大量 android-* 变体，被 `:app:debugRuntimeClasspath`（尤其 `runAlgoTests` 的 JavaExec classpath）解析时变体歧义、整构建 FAIL。改为 **Kotlin-JVM 纯库**（单一 jar 变体）后消除——Android/Wear 模块依赖纯 JVM 库是常态。值得沉淀为 skill。
  - 算法门禁 `runAlgoTests`（TestEngine/TestStats/TestFeatures 引用 `com.silema.app.data.*`/`engine.*`，TestStats 另引 `ppg.PpgAnalyzer`/`ble.BleCodec`，均留 `:app`）仍由 `:app` 跑，`:core` 类经 `debugRuntimeClasspath` 进 classpath。
- 手表端源码已落盘（`wear/` 下 11 文件）：MainActivity + WearApp(状态机导航) + theme/WearTheme + data/WearStore(本地 JSON) + ui/{Home,Entry,Sos,Workout,AiBrief}Screen；真实调用 `:core` 的 `RiskEngine.evaluate`/`HealthReport.weekly`，无 mock。SOS 仅本地确认（真实送达家人需后续蓝牙 DataClient 同步）；运动距离手表无 GPS 记 0（诚实）。
- 状态：`:core` 改为 Kotlin-JVM 后 `:app` 重编(8XYCYq)进行中，待绿后再把 `:wear` 接入 settings 并编译出 wear APK。手机端重定位为纯守护端（T23）尚未动手。仍未 git 提交。

## 继续推进（续）：手表端编译绿 + 手机端重定位守护端 + 双端出包
- `:wear` 编译验证：`assembleDebug` BUILD SUCCESSFUL（wear_build3.log）。仅 4 条 `ScalingLazyColumn` 弃用警告（已迁到 `androidx.wear.compose.foundation.lazy`，非阻塞）。手表端 5 屏全部真实调用 `:core`，0 代码、纯主产品成立。
- **T23 手机端重定位为纯守护端（已完成）**：`AppNav` 去掉老人家自用「录入」底部 tab 与适老化 SOS 常驻红色 FAB；底栏改为 监护/设备/报告/家人/更多（现代风，不老龄化）；NavHost 移除 ENTRY/WORKOUT/SOS 路由（体征录入/运动/呼救改由手表主产品承担）。`DashboardScreen`(首页监护看板) 删除屏内 SOS 自按压按钮 + 录入/运动快捷操作，四个快捷操作改为 设备/AI分析/医疗对接/家人监护（均守护相关），保留真实监测数据（风险评估/体征卡/预警/心率趋势）。`MoreScreen` 文案改为守护端导航、删「运动追踪」「紧急呼救：点击右下角红色按钮」。删除 `EntryScreen.kt`/`WorkoutScreen.kt`/`SosScreen.kt`（老人家自用屏，手表端已有）及其 Preview，消除死代码。
  - 编译踩坑：误删 `DashboardScreen` 的 `Color`/`BrandSoftRed` 导入（被 心率趋势卡 `Color.White`、预警数 `BrandSoftRed`、QuickAction/EntryNavCard 的 `Color` 参数复用）→ 首次 `assembleDebug` 报 9 个 `Unresolved reference`。补回两导入后重编 BUILD SUCCESSFUL。
- **T24 编译验证 + 算法门禁 + 双端出包（已完成）**：`./gradlew :wear:assembleRelease :app:assembleRelease runAlgoTests` BUILD SUCCESSFUL（3m14s）。算法门禁 `runAlgoTests` 三套全部 PASS（Engine 6 + Features 10 + Stats 18 = **34/34**）。`dist/` 刷新双端签名包：`silema-v0.5.0-release.apk`（手机守护端，12.9MB）、`silema-wear-v0.5.0-release.apk`（手表主产品，16.9MB）。
- 端架构定稿：手表端(Wear OS, minSdk30)=主产品/老人家佩戴；手机端(Android, minSdk26)=纯守护端/监视者。共享 `:core`(Kotlin-JVM) 领域层。仍未 git 提交（用户要求先不提交）。
